import { JSX } from "preact/jsx-runtime";

interface TabsProps {
  children: JSX.Element;
}

export function Tabs(props: TabsProps) {
  return (
    <section>
      <header>
      </header>
      <main>
      </main>
    </section>
  );
}
